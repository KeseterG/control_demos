import numpy as np
from scipy.sparse import csc_matrix
import pinocchio as pin

# Math
Vector = np.array
Matrix = np.array
SparseMatrix = csc_matrix

# Pinocchio Abbrs
PModel = pin.Model
PData = pin.Data
SE3 = pin.SE3

